<!-- Scaffolding generated by Members <%= members_get_full_version_string %> -->

<h2>View <%= singular_name.humanize.downcase %></h2>

<%%= form_for @<%= file_name %>, :url => members_<%= singular_name %>_path(@<%= singular_name %>) do |f| %>

	<%%= render :partial => 'members/<%= plural_name %>/form', :locals => { :f => f } %>
<% unless @read_only %>	
	<p class="submits">
	  	<%%= link_to 'Cancel', members_<%= @plural_route %>_path, :class => 'btn btn-mini btn-danger' %>
		<%%= f.submit "Save changes", :class => "btn btn-mini btn-primary" %>
	</p>
<% end %>
<%% end %>

<%%= content_for :sidebar do %>
	<li><%%= link_to "#{members_show_icon "th-list"}Back to list".html_safe, members_<%= @plural_route %>_path %></li>
<%% end %>